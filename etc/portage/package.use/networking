# SSL/TLS and SASL support
*/* ssl sasl

# IPv6 and HTTP/2 support where possible
*/* ipv6 http2

# No idea what this is useful for, but disable it anyway (shrug)
net-misc/openvpn -modules

# Prevent this being used as a DDoS relay
net-dns/nsd mmap ratelimit

# Completely unnecessary “accidental” backdoor
dev-libs/openssl -tls-heartbeat

# Run these more like services
net-irc/znc daemon
