#!/bin/sh
while true; do
    COUNT=$(/usr/bin/notmuch count 'tag:unread and (to:wichtig@nand.wakku.to or to:urgent@nand.wakku.to)')
    if [ $COUNT -gt 0 ]; then
        env printf "\uF003 ${COUNT}\n"
    else
        echo ''
    fi
    sleep 10
done
